{% extends 'login.base.html.twig' %}

{% block title %}Connexion - Mangapedia{% endblock %}

{% block body %}
    <div class="login-container">
        <div class="login-header">
            <div class="manga-icon">📚</div>
            <h1 class="login-title">Mangapedia</h1>
            <p class="login-subtitle">Connectez-vous à votre bibliothèque manga</p>
        </div>

        {% if app.user %}
            <div class="user-info">
                Vous êtes connecté en tant que <strong>{{ app.user.userIdentifier }}</strong>
                <br>
                <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </div>
        {% else %}
            <form method="post" class="login-form">
                {% if error %}
                    <div class="alert alert-danger">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}

                <div class="form-group">
                    <label for="username" class="form-label">Nom d'utilisateur</label>
                    <input 
                        type="text" 
                        value="{{ last_username }}" 
                        name="_username" 
                        id="username" 
                        class="form-control" 
                        autocomplete="username" 
                        required 
                        autofocus
                        placeholder="Entrez votre nom d'utilisateur"
                    >
                </div>

                <div class="form-group">
                    <label for="password" class="form-label">Mot de passe</label>
                    <input 
                        type="password" 
                        name="_password" 
                        id="password" 
                        class="form-control" 
                        autocomplete="current-password" 
                        required
                        placeholder="Entrez votre mot de passe"
                    >
                </div>

                <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

                {#
                <div class="remember-section">
                    <input type="checkbox" name="_remember_me" id="_remember_me">
                    <label for="_remember_me">Se souvenir de moi</label>
                </div>
                #}

                <button class="btn-primary" type="submit">
                    Se connecter
                </button>
            </form>

            <div class="auth-links">
                <a href="#" onclick="return false;">Mot de passe oublié ?</a>
            </div>
        {% endif %}
    </div>
{% endblock %}
