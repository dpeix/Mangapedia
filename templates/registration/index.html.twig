{% extends 'login.base.html.twig' %}

{% block title %}Inscription - Mangapedia{% endblock %}

{% block body %}
    <div class="login-container">
        <div class="login-header">
            <div class="manga-icon">📚</div>
            <h1 class="login-title">Mangapedia</h1>
            <p class="login-subtitle">Créez votre compte pour accéder à votre bibliothèque manga</p>
        </div>

        {% for flash_error in app.flashes('verify_email_error') %}
            <div class="alert alert-danger">{{ flash_error }}</div>
        {% endfor %}

        {% for flash_success in app.flashes('success') %}
            <div class="alert alert-info">{{ flash_success }}</div>
        {% endfor %}

        {{ form_start(registrationForm, {'attr': {'class': 'login-form'}}) }}
            
            <div class="form-group">
                {{ form_label(registrationForm.username, 'Nom d\'utilisateur', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.username, {
                    'attr': {
                        'class': 'form-control',
                        'placeholder': 'Choisissez votre nom d\'utilisateur',
                        'autofocus': true
                    }
                }) }}
                {{ form_errors(registrationForm.username) }}
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.plainPassword.first, 'Mot de passe', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.plainPassword.first, {
                    'attr': {
                        'class': 'form-control',
                        'placeholder': 'Créez un mot de passe sécurisé'
                    }
                }) }}
                {{ form_errors(registrationForm.plainPassword.first) }}
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.plainPassword.second, 'Confirmer le mot de passe', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.plainPassword.second, {
                    'attr': {
                        'class': 'form-control',
                        'placeholder': 'Confirmez votre mot de passe'
                    }
                }) }}
                {{ form_errors(registrationForm.plainPassword.second) }}
            </div>

            <button type="submit" class="btn-primary">
                Créer mon compte
            </button>
        {{ form_end(registrationForm) }}

        <div class="auth-links">
            Vous avez déjà un compte ? 
            <a href="{{ path('app_login') }}">Se connecter</a>
        </div>
    </div>
{% endblock %}
